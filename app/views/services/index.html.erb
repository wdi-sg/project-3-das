<p id="notice"><%= notice %></p>

<h1>Services</h1>

<div class="row">
  <% @services.each do |service| %>
    <div class="col-sm-6 col-md-4">
      <div class="thumbnail services">
        <%= link_to service do %>
          <%= image_tag service.image, width:419, height:219 if service.image.present? %>
        <% end %>
        <div class="caption">
          <h3><%= service.kind %></h3>
          <!-- <p><%= service.description %></p> -->
          <% if user_signed_in? && current_user.email == "admin@das.com" %>
            <%= link_to 'Edit', edit_service_path(service) %>
            <%= link_to 'Destroy', service, method: :delete, data: { confirm: 'Are you sure?' } %>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>

<br>

<% if user_signed_in? && current_user.email == "admin@das.com" %>
<%= link_to 'New Service', new_service_path %>
<% end %>
